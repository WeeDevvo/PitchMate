# PitchMate Watch (placeholder)

Native SwiftUI (watchOS) for live match tracking. **Not yet started** — backend + web come first.

When work begins here: the watch syncs via the paired phone and the API, and acts as one of several offline-first live-tracking clients (idempotent event log). See `.kiro/steering/` for the live-tracking decisions.
