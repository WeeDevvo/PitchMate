/**
 * LandingPage — placeholder scaffold for the marketing landing page feature.
 *
 * This is intentionally minimal. The real composition (theme provider, page
 * head, header, hero, benefit sections, closing CTA, footer) is built in later
 * tasks (see task 12 of the marketing-landing-page spec). For now it renders an
 * identifiable heading so the `/` route can be wired and verified.
 */
export function LandingPage() {
  return (
    <main>
      <h1>PitchMate</h1>
    </main>
  )
}

export default LandingPage
