# PitchMate Mobile (placeholder)

React Native + Expo (iOS + Android). **Not yet started** — backend + web come first.

When work begins here: Apple Sign-In is required for iOS, and the app consumes the same API via the generated `@pitchmate/api-client`. See `.kiro/steering/` for product and tech decisions.
